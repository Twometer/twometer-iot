AWS lambda for the Alexa skill

To make it compile, install requests package as follows:
 pip install requests -t ./

Make sure you don't use Windows 10 Store Python as that
breaks pip's -t. Ask me how I know.
