'use strict';

const db = require('../database')

module.exports = {
    namespace: 'Alexa.PowerController',
    handlers: {
        TurnOn(directive) {

        },

        TurnOff(directive) {

        }
    }
}